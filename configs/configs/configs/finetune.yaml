```
# Finetuning configuration
data_root: ./data
scene_list: ./data/scene_list_finetune.txt
output_root: ./outputs/finetune
gpu: 0
num_shots: 5
num_epochs: 20
learning_rate: 5e-4
batch_size: 128
pretrained_checkpoint: ./pretrained/contrastive_checkpoint.pt
```
